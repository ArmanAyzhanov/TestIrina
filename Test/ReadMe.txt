1) Изначально создаю панель с классом "panel panel-default" из bootstrap;
   Далее создаю таблицу, от которой принимаю элементы по id="table" в js;

2) Создаю массив книг books, с вложенными 4-мя книгами;

3) При загрузке страницы подгружаю две функции renderBooks(); renderAuthors();

4) Далее заполняю саму таблицу, задаю переменную bookRow, которая является заголовком,
   разметку для заголовка берет из функции renderBooks(), которая в коде по сути
   отрисовывает таблицу каждый раз. Далее создаются сами ряды таблицы: title, year,
   isbn, 2 кнопки. При задании onclick кнопкам использовал наложение функций из 
   обновленной документации.

5) При создании функции добавления книги addBook() добавляю книгу в массив и заново
   отрисовываю

6) При создании функции изменения книги editBook() вставляю измененную книгу на то же 
   место в массив и заново их отрисовываю

7) На delete убираю книгу из массива и заново все отрисовываю

   В 5,6,7 таблица стирается и каждый раз заново создается

Не разобрался: 

	Не удалось доделать динамическийй список авторов...
Хотел по той же логике, что с книгами, но что-то не выходит

	И еще с required аттрибутами у input'a что-то тоже не вышло.